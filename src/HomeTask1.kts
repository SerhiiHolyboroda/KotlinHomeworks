import kotlin.random.Random

fun main() {
     println("Hello, Kotlin/Native!");
    val nextValues = List(10) { Random.nextInt(0, 100) }
    println(nextValues)
    when(item.isOdd() -> println(it)
    println(it)
}